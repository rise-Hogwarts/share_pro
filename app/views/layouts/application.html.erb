<!DOCTYPE html>
<html>
  <head>
    <title>SharePro</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid bg-warning">
      <header>
        <div class="row">タンパク質を求める方に向けたレシピ投稿サイト</div>
        <div class="row">
          <div class="col-3">
            <h1 class="ml-5"><i class="fas fa-utensils"></i><u class="mx-3">share-pro</u><i class="fas fa-utensils"></i></h1>
          </div>
          <div class="col">
            <% if member_signed_in? %>
            <li class="nav-item">
              <%= link_to "ホーム画面", root_path, class:'btn btn-light' %>
            </li>
            <li class="nav-item">
              <%= link_to "ログアウト", destroy_member_session_path, method: :delete, class:'btn btn-light' %>
            </li>
            <% end %>
          </div>
          </div>
      </header>
    </div>
    <%= yield %>
  </body>
</html>
